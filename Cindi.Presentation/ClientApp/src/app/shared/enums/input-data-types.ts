export enum InputDataType {
  Int = 'int',
  String = 'string',
  Bool = 'bool',
  Object = 'object',
  ErrorMessage = 'errormessage',
  Decimal = 'decimal',
  DateTime = 'datetime',
  Secret = 'secret'
}
